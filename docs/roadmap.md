# Roadmap

The near-term roadmap is deliberately small:

- Harden local process wake/sleep behavior.
- Improve Ollama error messages and model detection.
- Add non-buffered streaming after v0.1 is reliable.
- Keep cloud/provider work out of the default build until it is complete and useful.
