# Echo Runtime

A tiny Python standard-library runtime used by the Emberlane v0.1 quickstart.

Supported endpoints:

- `GET /health`
- `POST /chat`
- `POST /echo`
- `POST /v1/chat/completions`

Run it directly:

```sh
PORT=9001 python server.py
```
