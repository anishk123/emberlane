# Simple Chat

This is a plain HTML demo for the local Emberlane HTTP API.

Run Emberlane:

```sh
cargo run -- init
cargo run -- serve
```

Open `examples/simple-chat/index.html` in a browser and send a message to `echo` or `ollama`.
